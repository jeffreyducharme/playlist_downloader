require 'spec_helper'

describe PlaylistDownloader do
  it 'has a version number' do
    expect(PlaylistDownloader::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
